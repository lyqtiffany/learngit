```python
#200925_分支语句
#分支语句又称条件判断语句
#输入一个分数,如果大于等于60,就显示"及格",否则显示不及格
# score=int(input('请输入:'))  #input()函数的返回值是str型
# if score>=60:
#     print('及格')
# else:
#     print('不及格')
#输入一个分数,如果大于等90,就显示"优秀",如果大于等80,就显示"不错",如果大于等于60,就显示"及格",否则显示不及格
# score=int(input('请输入:'))  #input()函数的返回值是str型
# if score>=90:
#     print('优秀')
# elif score>=80:
#     print('不错')
# elif score>=60:
#     print('及格')
# else:
#     print('不及格')
#if-elif-elif-else是一组,如果用if-if-if则每个if是独立的分支语句,互相之间没有联系
#elif语句与else语句是可以省略的,有需要时写,没有需要时可以不写
# if 1:  #除了0,False,None,以及空字符串''之外的值都是真
#     print('12345')  #占位符,当没有语句要写时,为了避免语法问题,就放一个pass在程序里

#分支语句的嵌套
#如果一个人的年龄大于等于60,且为男性,就打印"老先生"
# age=60
# gender='男'
# if age>=60 and gender=='男':
#     print('老先生')
# if age>=60:
#     if gender=='男':
#         print('老先生')

#思考题
# 1、用户控制台输入一个手机号，判断出运营商(移动（假设号段是130-150）、联通（假设是151-170）、电信（假设是171-199）)
# 2、输入位数不对，提示用户位数有误  len()函数,返回对象的长度
# 3、输入非数字，提示有非法字符（此处如果不会可以空着，但应先尝试找资料做一做）
# str1='1367890123R'
# print(str1.isdigit())  #判断是否纯数字
```